import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  emoji: {
    paddingBottom: 10,
    marginBottom: 5
  },
  image: {
    width: 240,
    height: 240
  },
})

export default styles